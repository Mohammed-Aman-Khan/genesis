{
  "name": "genesis-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "docs:dev": "cd apps && vitepress dev docs",
    "docs:build": "cd apps && vitepress build docs",
    "docs:preview": "cd apps && vitepress preview docs"
  },
  "devDependencies": {
    "turbo": "latest",
    "vitepress": "^2.0.0-alpha.14"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.2"
}
